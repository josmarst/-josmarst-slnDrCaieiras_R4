@model IEnumerable<atribuicaoAulas.Models.Turno>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descrTurno)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descrTurno)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTurno }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTurno }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTurno })
        </td>
    </tr>
}

</table>
